/**
 * @description this is a workaround ->
 * somehow parallel pages have issue when first folder item throws an error, then the error are not catched by the error.tsx ...
 **/
export default function Page() {
  return null;
}
