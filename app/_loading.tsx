/**
 * 🥲 very sadly next.js has issues when navigating in child routes
 * and shows the global loading component, thats why we set a empty
 * loading component here to avoid flickering...
 */
export default function Page() {
  return null;
}
